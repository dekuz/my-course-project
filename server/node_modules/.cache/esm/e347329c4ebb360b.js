let express,cors,readdirSync,mongoose;_4eb‍.w("express",[["default",["express"],function(v){express=v}]]);_4eb‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_4eb‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_4eb‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);



const morgan = require("morgan");
require("dotenv").config();

//create express app
const app = express();

// db
// mongoose.connect(process.env.DATABASE,{
//     useNewUrlParser: true,
//     useFindAndModify:false,
//     useUnifiedTopology:true,
//     userCreateIndex:true,
// });


mongoose.connect('mongodb://localhost:27017/edemy',
  {
    useNewUrlParser: true,
    useFindAndModify: false,
    useUnifiedTopology: true
  }
);


//apply middlewares
app.use(cors());
app.use(express.json());
app.use(morgan("dev"));

//route
readdirSync('./routes').map((r)=>
    app.use('/api', require(`./routes/${r}`))
);

//setup port
const port = process.env.PORT||8000;

app.listen(port,()=>console.log(`Server is running in port ${port}`))
